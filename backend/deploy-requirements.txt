# Packages installed into the Pi venv during deploy.
# - pygame is excluded: provided by system (/usr/lib/python3/dist-packages) via .pth
# - RPi.GPIO is excluded: use system rpi-lgpio instead
# - uvicorn[standard] is avoided: uvloop has no armv7l wheel
# - orjson is excluded: Rust extension, no armv7l wheel
fastapi>=0.111
uvicorn>=0.29
pydantic-settings>=2.2
python-dotenv>=1.0
requests>=2.31
tinytuya>=1.15
alembic>=1.13
typing_extensions>=4.10
pytest>=8.2
pytest-asyncio>=0.23
httpx>=0.27
